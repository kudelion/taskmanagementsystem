<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">  
    <title>Task Management System | View Task</title>
</head>
<body>
    <div class="container mt-5">
        <h1>Task Management System</h1>
        <div class="d-flex justify-content-between align-items-center mb-4">    
            <h2>Task Information</h2>
        </div>
            <div class="form-group mb-3">
                <label for="task">Task:</label>
                <input type="text" class="form-control" id="task" name="task" value="<%=task.task%>" disabled>
            </div>
            <div class="form-group mb-3">
                <label for="description">Description:</label>
                <textarea class="form-control" id="description" name="description" disabled><%= task.description %></textarea>
            </div>
            <div class="form-group mb-3">
                <label for="assignTo">Assign to:</label>
                <input type="text" class="form-control" id="assignTo" name="assignTo" value="<%= task.assignTo %>"disabled>
            </div>
            <div class="form-group mb-3">
                <label for="dueDate">Due date:</label>
                <% const dueDate = new Date(task.dueDate); %>
                <% const formatteddueDate = `${dueDate.getMonth()+1}/${dueDate.getDate()}/${dueDate.getFullYear()}`; %>
                <input type="text" class="form-control" id="dueDate" name="dueDate"  value="<%= formatteddueDate %>" disabled>
            </div>
            <div class="form-group mb-3">
                <label for="taskStatus">Task Status:</label>
                <input type="text" class="form-control" id="taskStatus" value="<%= task.taskStatus === false? "Open" : "Closed" %>" disabled>
            </div>
            <a href="/tasks" class="btn btn-secondary">Back</a>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM" crossorigin="anonymous"></script>

</body>
</html>