<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">  
    <title>Task Management System | Edit Task</title>
    <style>
        .required::after {
            content: "*";
            color: red;
            margin-left: 5px;
        }
    </style>
</head>
<body>
    <div class="container mt-5">
        <h1>Task Management System</h1>
        <div class="d-flex justify-content-between align-items-center mb-4">    
            <h2>Update Task Information</h2>
        <form action="/tasks/<%=task._id%>?_method=PUT" method="post">
        </div>
        <div class="form-group mb-3">
            <label for="task">Task ID:</label>
            <input type="text" class="form-control" id="task" name="task" value="<%=task._id%>" disabled>
        </div>
        <div class="form-group mb-3">
            <label for="task">Task:</label>
            <input type="text" class="form-control" id="task" name="task" value="<%=task.task%>" disabled>
        </div>
        <div class="form-group mb-3">
            <label for="description" class="required">Description:</label>
            <textarea class="form-control" id="description" name="description" required><%= task.description %></textarea>
        </div>
        <div class="form-group mb-3">
            <label for="assignTo" class="required">Assign to:</label>
            <input type="text" class="form-control" id="assignTo" name="assignTo" value="<%= task.assignTo %>"required>
        </div>
        <div class="form-group mb-3">
            <label for="dueDate" class="required">Due date:</label>
            <% const dueDate = new Date(task.dueDate); %>
            <% const formatteddueDate = `${dueDate.getMonth()+1}/${dueDate.getDate()}/${dueDate.getFullYear()}`; %>
            <input type="text" class="form-control" id="dueDate" name="dueDate"  value="<%= formatteddueDate %>" required>
        </div>
        <div class="form-group mb-3">
            <label for="taskStatus"class="required">Task Status:</label>
            <select class="form-control" id="taskStatus" name="taskStatus" required>
                <option value="false" <%= task.taskStatus === false ? 'selected' : '' %>>Open</option>
                <option value="true" <%= task.taskStatus === true ? 'selected' : '' %>>Closed</option>  
            </select>
        </div>
        <button type="submit" class="btn btn-primary">Update</button>
        <a href="/tasks" class="btn btn-secondary">Back</a>
        </form>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM" crossorigin="anonymous"></script>
</body>
</html>